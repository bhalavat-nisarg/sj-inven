import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bit-fields.sj',
  appName: 'SJ Inventory',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
